const doc = document.getElementById('redirect');

const href;

var _html = href + ' , if you are not redirected by then <a href=' + href + '>click me</a>'

doc.innerHTML += _html


window.location = href;
